# Dependencies
node_modules/

# Build output
dist/

# Data files — personal match history & decks (don't commit private data)
data/decks.json
data/matches.json
data/collection.json

# Card cache — large auto-generated file, not needed in repo
data/card-cache.json
data/live-meta.json

# Keep the data directory itself
!data/.gitkeep

# Environment / local config
.env
.env.local

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor config
.vscode/settings.json
.idea/

# Logs
*.log
npm-debug.log*
